language: node_js
node_js:
  - "7"
cache: npm
script:
  - echo "Deploy!!"
  - pushd functions
  - npm i
install:
  - npm install -g firebase-tools
after_success:
  - firebase deploy --token $FIREBASE_TOKEN
